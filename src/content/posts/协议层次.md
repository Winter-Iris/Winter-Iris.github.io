---
title: 协议层次
description: 网络协议的分层体系结构与服务模型
published: 2026-06-27
category: 计算机网络
tags:
    - 计算机网络
---

# 分层的协议体系

- 网络设计者以**分层**(layer)的方式组织协议以及实现这些协议的网络硬件和软件。
- **服务(Service)**：**底层实体向上层实体提供**它们之间通信的能力。
- 原语：上层实体使用原语调用下层实体提供的服务。
- 服务用户：接受并使用服务提供商提供的服务
- 服务提供商：提供服务给服务用户。
- 服务访问点（SAP）：上层实体使用下层实体提供的服务的接口。
- 服务分为：面向连接的服务，无连接的服务。
- 本层协议的实现使用了下层提供的服务，为了向上层实现更好的服务。

# 数据单元（DU）

- Data Unit，泛指网络中传输的**一组**数据单元。
- **SDU**（Service Data Unit）：**服务数据单元**，这是从**上层**传下来的原始数据。
- **PDU**（Protocol Data Unit）：**协议数据单元**，这是每一层**对等实体**之间交换的数据单位。
- 第n层**接受来自上一层的SDU**，经过处理，在SDU单元头部**加上来自当前层的Header**，**成为当前层的PDU**。
- 不同层次的 PDU 有自己专属的名称。


# 协议栈

- 对于计算机网络各层的所有协议综合起来被称为**协议栈**
- 从上到下依次为：应用层，传输层，网络层，链路层，网络层

## 物理层

- 物理层的任务是**将**从上层传输下来的帧中的一个个**比特（bit）从一个节点移动到下一个节点**。

## 链路层

- **链路层分组**称为**帧**(**frame**)。
- 网络层将数据报下传给链路层，链路层沿着路径将数据报传递给下一个节点。在该下一个节点，链路层将数据报上传给网络层。
- 链路层解决的是相邻两个**节点之间的帧的传输**。

## 网络层

- 因特网的网络层负责将称为**数据报**(**datagram**)的网络层分组**从一台主机移动到另一台主机**。

## 传输层

- 因特网的**传输层**在**应用程序端点之间**传送应用层报文。在因特网中，有两种运输协议，即TCP和UDP,利用其中的任一个都能运输应用层报文。
- TCP向它的应用程序提供了**面向连接**的服务。这种服务包括了应用层报文向目的地的确保传递和**流量控制**（即发送方/接收方速率匹配)。TCP也将长报文划分为短报文，并提供**拥塞控制**机制，因此当网络拥塞时，源抑制其传输速率。
- UDP协议向它的应用程序提供**无连接服务**。这是一种不提供不必要服务的服务，没有可靠性，没有流量控制，也没有拥塞控制。 
- 运输层的分组称为**报文段**(**segment**)。

## 应用层

- 在传输层协议的基础上实现各种**网络应用**。应用层的PDU称为**报文（message）**。


# OSI 模型

- 将计算机网络分为七层：
- 应用层、**表示层、会话层**、运输层、网络层、数据链路层和物理层
- **表示层**的作用是使**通信的应用程序能够解释交换数据的含义**。
- **会话层**提供了**数据交换的定界和同步功能**，包括了建立检查点和恢复方案的方法。

# 封装

- 在发送主机端，一个应用层报文被传送给运输层，在最简单的情况下，运输层收取到报文并附上附加信息（所谓运输层首部信息），应用层报文和运输层首部信息一道构成了运输层报文段。运输层报文段因此封装了应用层报文。
- 运输层则向网络层传递该报文段，网络层增加了如源和目的端系统地址等网络层首部信息，生成了网络层数据报
- 该数据报接下来被传递给链路层，链路层（自然而然地）增加它自己的链路层首部信息并生成链路层帧(link-layer frame)。
- 在每一层，一个分组具有两种类型的字段：**首部字段和有效载荷字段**(payload field)。**有效载荷通常是来自上一层的分组**。